# おっ！サン コラボ運用

設定は `collaboration-config.js`。初期状態は有効、日時未指定の手動開催です。開始・終了を指定する場合は `2026-10-01T00:00:00+09:00` のようにタイムゾーンを含めてください。開始前・終了後は入口も画像も表示しません。プレイ中の終了、アプリ復帰時にも判定します。判定時刻は端末時計です。

## 仕様

- 通常20ステージでSUN POWERを表示。デイリー・練習は対象外。
- コラボバナーは説明専用。専用ステージ・45秒チャレンジ・チャレンジ報酬はありません。過去に獲得したコインや保存済みの報酬データは変更しません。
- PERFECT +14%、GREAT +9%、OK（GOOD相当）+5%。MISSは増加なし。同じノーツでの二重蓄積なし。
- 100%になると通常プレイを続けたまま10秒のSUN FEVERを発動。音楽・進行・操作は切り替えません。
- 左右入力の特殊ゲーム、専用打音、FINAL同時押し、特殊ゲーム用ボーナスはありません。
- FEVER中は無敵、通常スコア・コイン2倍。終了後1秒の保護が残ります。プレイヤー周囲にレインボーリングを表示します。
- 公式元画像を進行バーの左に表示。左端から画面幅の12％、上端から22論理pxの余白を取り、高さ72論理pxまで表示します。反転コースや上側のコインが近づいても表示サイズを維持します。デザイン・色・縦横比を変更しません。セリフ・ボイスは追加しません。
- 発動時にゲージをリセット。FEVER終了後に再び成功判定で蓄積できます。
- コラボOFF時は通常の7秒FEVERに戻ります。通常記録・コインは保持します。

## 変更箇所・確認

index.htmlは既存FEVER・衝突・描画への接続、sun-rhythm.jsはSUNゲージと背景演出、collaboration.jsはコラボ説明・期間管理を担当します。
scripts/test-collaboration.jsで音楽と進行の継続、従来のジャンプ操作、ゲージ、無敵、コイン2倍、終了・再蓄積、無効化を確認します。

## 終了手順

```powershell
npm run collab:disable
npm run web:build
```

`www/` が終了版の配布物です。コラボ素材が含まれないことをビルド処理が検査します。Webは **wwwを公開対象** にして更新してください。リポジトリ全体を公開すると原本も配信されるため、終了版配布物になりません。

iOSは `npm run cap:sync:ios` 後、通常の署名・アーカイブ・配布を行います。旧インストール内の素材を遠隔削除する機能はありません。終了日時を設定した旧版は端末時計に従って非表示になり、素材自体の除去には更新が必要です。

再度有効にする場合は `npm run collab:enable`、必要に応じて日時を更新して再ビルドしてください。

## 配布・確認

## UI構成

フィーバー背景は公式素材7枚を `10 → 07 → 08 → 09 → 11 → 12 → 13` の順に発動ごとに切り替え、全種類表示後に一周します。リトライでも順番を維持し、ページ再読み込みで10に戻ります。縦横比・色・画像全体を保持し、画像内のパーツやセリフは追加しません。

`ui.js` / `ui.css` は通常版・コラボ版共通の表示レイヤーです。ホームは「つづきから」を主ボタンとし、キャラクター・ミッション・その他は個別パネルに整理しています。プレイ中は上部に進行とスコア、下部にSUN POWER（コラボ無効時は従来FEVER）を表示します。ステージ名と能力説明は開始前だけ、コンボは10回ごとの節目に表示します。リザルトはランク・スコア・報酬・次の操作の順にまとめています。コラボ終了後もこのUIは維持されます。

## ビルド確認

ビルドは生成先 `www/` を再生成します。ソースの `Asset/collaborations/` は保持します。有効版でも配布対象は `ohsun/characters/ohsun_07.png` ～ `ohsun_13.png` の7枚のみ。README、原本保管用source、確認資料review、未採用素材は含めません。コラボ画像はService Workerの実行時キャッシュに保存しません。

確認用ビルドの公開は行わず、ローカルで検証します。回帰確認用 `scripts/test-collaboration.js` はPlaywrightを使用します。
